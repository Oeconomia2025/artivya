[build]
  publish = "dist/public"
  command = "npm install --include=dev && npx vite build --config vite.config.netlify.ts"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
